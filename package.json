{
  "name": "@nofrixion/business",
  "version": "0.0.0",
  "license": "MIT",
  "description": "You won't actually become a bank: you'll just forget you needed one.",
  "author": "NoFrixion",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"{packages,apps}/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "prettier": "^3.0.1"
  }
}
